

model User {
    id          Int         @id @default(autoincrement())
    email       String      @unique
    password    String
    hashedRT    String?      @unique
    createdAt   DateTime    @default(now())
}
